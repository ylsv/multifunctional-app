import {getUIScrollByPath} from './model/selectors/getUi'
import {uiReducer, uiActions} from './model/slice/uiSlice'
import {UISchema} from './model/types/UISchema'

export {
  UISchema,
  getUIScrollByPath,
  uiReducer,
  uiActions,
}
